
  # Simple Dice Game

  This is a code bundle for Simple Dice Game. The original project is available at https://www.figma.com/design/HB4LlSFDTpyjT958PF0IeQ/Simple-Dice-Game.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  